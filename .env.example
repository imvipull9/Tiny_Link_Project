# Port where backend runs
PORT=5000

# Postgres connection string
# Example (Neon / Render / Railway / local Postgres)
DATABASE_URL=postgres://user:password@host:5432/Task

# Frontend origin for CORS
CLIENT_ORIGIN=http://localhost:3000

# Base URL for short links (for display; used by frontend too if needed)
BASE_URL=http://localhost:5000
